/**
 * Checks if given error is a LibrdKafkaError. Small helper we might will remove later on.
 *
 * @param error {unknown}
 *
 * @returns {boolean}
 */
export declare function isLibrdKafkaError(error: unknown): boolean;
