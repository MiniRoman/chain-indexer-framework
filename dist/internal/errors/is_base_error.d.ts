/**
 * Check if the given error is BaseError. Small helper we might will remove later on.
 *
 * @param error {unknown}
 *
 * @returns {boolean}
 */
export declare function isBaseError(error: unknown): boolean;
